# 🔧 פתרון שגיאת I/O ב-Docker

## מה השגיאה?
```
input/output error: write /var/lib/docker/buildkit/containerd-overlayfs/metadata_v2.db
```

זו שגיאה נפוצה ב-Docker Desktop ב-Windows, בדרך כלל נגרמת מ:
- Cache פגום של Docker
- בעיה ב-Docker daemon
- בעיית זכרון/דיסק

---

## פתרונות (נסה לפי הסדר)

### פתרון 1: ניקוי Cache של Docker
```powershell
# עצור את Docker Desktop קודם (מ-Docker Desktop UI)

# נקה את ה-build cache
docker builder prune -a -f

# נקה את כל ה-containers וה-images שלא בשימוש
docker system prune -a -f
```

### פתרון 2: איפוס Docker Desktop
1. סגור את Docker Desktop לחלוטין
2. פתח שוב את Docker Desktop
3. חכה ש-Docker יתחיל (האיקון ירוק)
4. נסה שוב:
```powershell
docker compose up --build
```

### פתרון 3: איפוס מלא של Docker
```powershell
# עצור את Docker Desktop קודם

# נקה הכל
docker system prune -a --volumes -f

# או דרך Docker Desktop:
# Settings > Troubleshoot > Clean / Purge data
```

### פתרון 4: הפעלה מחדש של Docker Desktop
1. סגור את Docker Desktop לחלוטין
2. פתח Task Manager (Ctrl+Shift+Esc)
3. מצא את כל התהליכים של Docker
4. סיים אותם
5. הפעל מחדש את Docker Desktop
6. חכה שהכל יעלה
7. נסה שוב

### פתרון 5: בנייה ללא cache
```powershell
docker compose build --no-cache
docker compose up
```

### פתרון 6: איפוס WSL (אם משתמש ב-WSL2)
```powershell
wsl --shutdown
# הפעל מחדש את Docker Desktop
```

---

## פתרון מהיר (מומלץ לנסות קודם)

```powershell
# 1. עצור את Docker Desktop

# 2. נקה cache
docker builder prune -a -f

# 3. הפעל מחדש את Docker Desktop

# 4. נסה שוב
docker compose up --build
```

---

## אם כלום לא עובד

### איפוס מלא דרך Docker Desktop:
1. פתח Docker Desktop
2. לחץ על האיקון ⚙️ (Settings)
3. לחץ על "Troubleshoot"
4. לחץ על "Clean / Purge data"
5. הפעל מחדש את Docker Desktop
6. נסה שוב

---

## בדיקות לפני ניסיון חוזר

```powershell
# בדוק ש-Docker עובד
docker --version
docker ps

# בדוק שיש מספיק מקום בדיסק
# (פתח This PC ובדוק את הדיסק C:)
```

---

## טיפים למניעה

1. **סגור containers שלא בשימוש:**
   ```powershell
   docker compose down
   ```

2. **נקה cache מדי פעם:**
   ```powershell
   docker system prune -f
   ```

3. **ודא שיש מספיק מקום בדיסק** (לפחות 10GB פנוי)

4. **ודא ש-Docker Desktop מעודכן** לגרסה האחרונה

