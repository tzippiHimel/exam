# 🚀 הרצה ללא Docker - הוראות מפורטות

## דרישות מוקדמות

1. **Python 3.11+** - [הורדה](https://www.python.org/downloads/)
2. **Node.js 18+** - [הורדה](https://nodejs.org/)
3. **Git** (אופציונלי)

---

## חלק 1: Backend (Python/FastAPI)

### שלב 1: התקנת Python dependencies

```powershell
cd backend
python -m venv venv
```

### שלב 2: הפעלת Virtual Environment

**Windows:**
```powershell
venv\Scripts\activate
```

**Linux/Mac:**
```bash
source venv/bin/activate
```

### שלב 3: התקנת חבילות Python

```powershell
pip install --upgrade pip
pip install -r requirements.txt
```

### שלב 4: התקנת Tesseract OCR (חובה!)

**Windows:**
1. הורד מ: https://github.com/UB-Mannheim/tesseract/wiki
2. התקן (למשל: `C:\Program Files\Tesseract-OCR`)
3. הוסף ל-PATH או הגדר משתנה סביבה:
   ```powershell
   $env:PATH += ";C:\Program Files\Tesseract-OCR"
   $env:TESSDATA_PREFIX = "C:\Program Files\Tesseract-OCR\tessdata"
   ```

**Linux (Ubuntu/Debian):**
```bash
sudo apt-get update
sudo apt-get install tesseract-ocr libtesseract-dev poppler-utils
```

**Mac:**
```bash
brew install tesseract poppler
```

### שלב 4.5: התקנת Poppler (נדרש לעיבוד PDF)

**Windows:**
1. הורד Poppler מ: https://github.com/oschwartz10612/poppler-windows/releases/
   - הורד את הגרסה האחרונה (למשל: `Release-XX.XX.X-X.zip`)
2. חלץ את הקובץ (למשל: `C:\poppler`)
3. הוסף את התיקייה `bin` ל-PATH:
   ```powershell
   # זמני (למשך הסשן הנוכחי):
   $env:PATH += ";C:\poppler\Library\bin"
   
   # קבוע (הוסף ל-PATH של Windows):
   # לחץ Win+R, הקלד "sysdm.cpl", לחץ על "Environment Variables"
   # הוסף את C:\poppler\Library\bin ל-PATH
   ```
4. בדוק שההתקנה עבדה:
   ```powershell
   pdftoppm -h
   ```
   (אמור להציג עזרה)

**Linux (Ubuntu/Debian):**
```bash
sudo apt-get install poppler-utils
```

**Mac:**
```bash
brew install poppler
```

**הערה:** אם לא מתקינים Poppler, עיבוד PDF לא יעבוד. במקרה כזה, השתמשו בתמונות (.png, .jpg) או קבצי טקסט (.txt) במקום PDF.

### שלב 5: הגדרת משתני סביבה

צור קובץ `.env` בתיקיית `backend`:

```env
GEMINI_API_KEY=המפתח_שלך_כאן
OCR_LANGUAGE=en
MAX_FILE_SIZE_MB=10
```

### שלב 6: הרצת Backend

```powershell
uvicorn app.main:app --reload --host 0.0.0.0 --port 8000
```

✅ **Backend רץ על:** http://localhost:8000
📖 **תיעוד API:** http://localhost:8000/docs

---

## חלק 2: Frontend (React)

### שלב 1: התקנת Node dependencies

פתח טרמינל **חדש** (בלי לסגור את Backend):

```powershell
cd frontend
npm install
```

### שלב 2: הרצת Frontend

```powershell
npm start
```

✅ **Frontend רץ על:** http://localhost:3000

---

## סדר פעולות מלא

### טרמינל 1 - Backend:
```powershell
cd backend
python -m venv venv
venv\Scripts\activate
pip install -r requirements.txt
# ודא ש-Tesseract מותקן!
uvicorn app.main:app --reload
```

### טרמינל 2 - Frontend:
```powershell
cd frontend
npm install
npm start
```

---

## פתרון בעיות

### שגיאת Tesseract לא נמצא:
```powershell
# Windows - הוסף ל-PATH או הגדר:
$env:PATH += ";C:\Program Files\Tesseract-OCR"
```

### שגיאת port תפוס:
```powershell
# שנה פורט ב-backend:
uvicorn app.main:app --reload --port 8001

# או ב-frontend:
# שנה ב-package.json את ה-proxy
```

### שגיאת EasyOCR:
```powershell
# EasyOCR דורש Tesseract מותקן
# ודא שהתקנת Tesseract לפני pip install
```

### שגיאת Gemini API:
```powershell
# ודא שקובץ .env קיים ב-backend עם GEMINI_API_KEY
Get-Content backend\.env
```

### שגיאת Poppler (PDF processing failed):
```powershell
# Windows - ודא ש-Poppler מותקן וב-PATH:
pdftoppm -h

# אם לא עובד, הוסף ל-PATH:
$env:PATH += ";C:\poppler\Library\bin"

# או השתמש בתמונות (.png, .jpg) במקום PDF
```

---

## בדיקה שהכל עובד

1. **Backend:** פתח http://localhost:8000/api/health
   - צריך לראות: `{"status":"healthy"}`

2. **Frontend:** פתח http://localhost:3000
   - צריך לראות את הממשק

3. **API Docs:** פתח http://localhost:8000/docs
   - צריך לראות את Swagger UI

---

## עצירת השירותים

- **Backend:** לחץ `Ctrl+C` בטרמינל
- **Frontend:** לחץ `Ctrl+C` בטרמינל

---

## הערות חשובות

⚠️ **Tesseract OCR חובה!** - EasyOCR לא יעבוד בלי Tesseract מותקן

⚠️ **קובץ .env** - צריך להיות בתיקיית `backend/` עם `GEMINI_API_KEY`

⚠️ **שני טרמינלים** - צריך להריץ Backend ו-Frontend בנפרד

