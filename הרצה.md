# 🚀 הוראות הרצה - Exam Grading System

## שלב 1: בניית ה-Images (בפעם הראשונה)

```powershell
docker compose build
```

**או לבנות רק backend:**
```powershell
docker compose build backend
```

**או לבנות רק frontend:**
```powershell
docker compose build frontend
```

⏱️ **זה יקח 5-10 דקות בפעם הראשונה** (הורדת תלויות ומודלים)

---

## שלב 2: הרצת השירותים

```powershell
docker compose up
```

**או להריץ ברקע:**
```powershell
docker compose up -d
```

**או לבנות ולהריץ יחד:**
```powershell
docker compose up --build
```

---

## שלב 3: גישה לאפליקציה

לאחר שהכל עולה (תראה "Application startup complete"):

- **ממשק משתמש**: http://localhost:3000
- **API תיעוד**: http://localhost:8000/docs
- **API ישיר**: http://localhost:8000

---

## פקודות שימושיות

### צפייה בלוגים
```powershell
# כל הלוגים
docker compose logs

# רק backend
docker compose logs backend

# רק frontend
docker compose logs frontend

# לוגים בזמן אמת (follow)
docker compose logs -f
```

### בדיקת סטטוס
```powershell
docker compose ps
```

### עצירת השירותים
```powershell
# עצירה רגילה
docker compose down

# עצירה + מחיקת volumes
docker compose down -v
```

### הפעלה מחדש
```powershell
docker compose restart
```

### בנייה מחדש (אחרי שינויים בקוד)
```powershell
docker compose up --build
```

---

## פתרון בעיות

### אם יש שגיאת build:
```powershell
# נקה ובנה מחדש
docker compose down
docker compose build --no-cache
docker compose up
```

### אם פורט תפוס:
```powershell
# בדוק מה משתמש בפורט
netstat -ano | findstr :3000
netstat -ano | findstr :8000

# או שנה פורטים ב-docker-compose.yml
```

### אם יש שגיאת API:
```powershell
# בדוק את קובץ .env
Get-Content .env

# ודא שיש GEMINI_API_KEY תקין
```

---

## סדר פעולות מומלץ

1. **בדוק ש-Docker רץ:**
   ```powershell
   docker --version
   ```

2. **בדוק שקובץ .env קיים:**
   ```powershell
   Test-Path .env
   Get-Content .env
   ```

3. **בנה והרץ:**
   ```powershell
   docker compose up --build
   ```

4. **פתח בדפדפן:**
   - http://localhost:3000

---

## ✅ סימנים שהכל עובד

- **Backend**: תראה "Uvicorn running on http://0.0.0.0:8000"
- **Frontend**: תראה "webpack compiled successfully"
- **בדיקה**: פתח http://localhost:8000/api/health - צריך לראות `{"status":"healthy"}`

