# 📋 שלבים להרצת האפליקציה

## שלב 1: בנייה (Build)
```powershell
docker compose build
```
⏱️ **זה יקח 5-10 דקות בפעם הראשונה**

---

## שלב 2: הרצה (Run)
```powershell
docker compose up
```

**או להריץ ברקע:**
```powershell
docker compose up -d
```

⏱️ **זה יקח 1-2 דקות עד שהכל יעלה**

---

## שלב 3: בדיקה שהכל עובד

### בדיקת Backend:
פתח בדפדפן: **http://localhost:8000/api/health**

צריך לראות:
```json
{"status":"healthy","service":"exam-grading-api"}
```

### בדיקת Frontend:
פתח בדפדפן: **http://localhost:3000**

צריך לראות את הממשק עם הכותרת "AI Exam Grading System"

### בדיקת API Docs:
פתח: **http://localhost:8000/docs**

צריך לראות את Swagger UI עם כל ה-endpoints

---

## שלב 4: שימוש באפליקציה

1. **העלאת מבחן פתור**
   - פתח: http://localhost:3000
   - לחץ "Browse" ובחר קובץ (PDF/תמונה/טקסט)
   - לחץ "Upload Exam"

2. **פיענוח המבחן**
   - לחץ "Parse Exam"
   - חכה שהמערכת תחלץ את השאלות

3. **הגשת תשובות**
   - הזן את תשובות התלמיד לכל שאלה
   - לחץ "Submit Answers for Grading"

4. **צפייה בתוצאות**
   - ראה את הציון הסופי
   - ראה פירוט לכל שאלה עם הסברים

---

## פקודות שימושיות במהלך השימוש

### צפייה בלוגים בזמן אמת:
```powershell
docker compose logs -f
```

### עצירת האפליקציה:
```powershell
docker compose down
```

### הפעלה מחדש:
```powershell
docker compose restart
```

### בנייה מחדש (אחרי שינויים):
```powershell
docker compose up --build
```

---

## 🎯 סדר פעולות מלא (Copy-Paste)

```powershell
# 1. בנייה
docker compose build

# 2. הרצה
docker compose up

# 3. פתח בדפדפן:
# http://localhost:3000
```

**או הכל יחד:**
```powershell
docker compose up --build
```

---

## ⚠️ אם יש בעיות

### בדיקת לוגים:
```powershell
docker compose logs backend
docker compose logs frontend
```

### עצירה והתחלה מחדש:
```powershell
docker compose down
docker compose up --build
```

### בדיקת סטטוס:
```powershell
docker compose ps
```

